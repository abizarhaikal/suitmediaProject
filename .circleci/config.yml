version: 2.1

orbs:
  android: circleci/android@0.2.1

jobs:
  build:
    executor: android/android
    steps:
      - checkout
      - run:
          name: Install Java 17
          command: sudo apt-get update && sudo apt-get install openjdk-17-jdk
      - run:
          name: Set JAVA_HOME to Java 17
          command: echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64' >> $BASH_ENV
      - restore_cache:
          key: android-orb-v1-{{ checksum "gradle.lockfile" }}
      - run:
          name: Chmod permissions
          command: sudo chmod +x ./gradlew
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies --no-daemon
      - save_cache:
          key: 'android-orb-v1-{{ checksum "gradle.lockfile" }}'
          paths:
            - ~/.android/build-cache
            - ~/.gradle/caches/
            - ~/.gradle/wrapper/
      - run:
          name: Run Build
          command: ./gradlew build --no-daemon --stacktrace
      - store_artifacts: 
          path: app/build/reports
          destination: reports
      - run:
          name: Run Lint
          command: ./gradlew lint --no-daemon --stacktrace
      - run:
          name: Run Unit Tests
          command: ./gradlew test --no-daemon --stacktrace
      - store_test_results:
          path: app/build/test-results
      - store_artifacts:  
          path: app/build/outputs/apk/debug/
          destination: artifact-file
